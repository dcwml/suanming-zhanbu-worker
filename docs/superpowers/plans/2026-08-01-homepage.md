# 首页改版 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将占位首页改版为四板块正式首页（Hero → 工具卡片 → 天干地支 → 五行），中英双语对称，纯静态无 JS。

**Architecture:** 只改内容片段与样式——重写 `src/content/home.zh.html` / `home.en.html` 两个 HTML 片段，在 `public/assets/style.css` 追加首页分组样式，更新 `registry.ts` 首页 meta description。路由、布局、SEO 管线零改动（首页 slug `""` 已注册，一切自动派生）。

**Tech Stack:** HTML 片段（wrangler Text rules 导入）、CSS（无预处理器）、Vitest + @cloudflare/vitest-pool-workers（SELF.fetch 集成测试）

**Spec:** `docs/superpowers/specs/2026-08-01-homepage-design.md`

---

## File Structure

| 文件 | 职责 | 操作 |
|---|---|---|
| `test/integration.test.ts` | 新增 2 个首页 CTA 链接断言（"pages" describe 块内） | Modify |
| `src/content/home.zh.html` | 中文首页正文片段（4 板块） | Rewrite |
| `src/content/home.en.html` | 英文首页正文片段（4 板块） | Rewrite |
| `public/assets/style.css` | 追加 `/* ---------- 首页 ---------- */` 分组 | Modify |
| `src/pages/registry.ts` | 首页 meta.description 更新（去掉"塔罗占卜"） | Modify |

---

### Task 1: 首页 CTA 链接集成测试（TDD 红灯）

**Files:**
- Modify: `test/integration.test.ts`（"pages" describe 块，"renders en sample page" 测试之后插入）

- [ ] **Step 1: 写失败测试**

在 `test/integration.test.ts` 的 `describe("pages", ...)` 块中，`"renders en sample page"` 测试之后插入：

```ts
  it("zh home renders tool cards with CTA links", async () => {
    const res = await fetchNoFollow("/zh/");
    expect(res.status).toBe(200);
    const html = await res.text();
    expect(html).toContain('class="tool-cta" href="/zh/bazi/"');
    expect(html).toContain('class="tool-cta" href="/zh/liuyao/"');
  });

  it("en home renders tool cards with CTA links", async () => {
    const res = await fetchNoFollow("/en/");
    expect(res.status).toBe(200);
    const html = await res.text();
    expect(html).toContain('class="tool-cta" href="/en/bazi/"');
    expect(html).toContain('class="tool-cta" href="/en/liuyao/"');
  });
```

注意：不能只断言 `href="/zh/bazi/"`——导航栏也渲染同样的链接（见 `src/layout/nav.ts`）。`class="tool-cta"` 是首页内容片段独有的标记，确保断言命中正文而非导航。断言字符串要求 HTML 中属性顺序为 class 在前、href 在后（Task 2 的内容片段按此顺序书写）。

- [ ] **Step 2: 运行测试，确认失败**

Run: `npx vitest run test/integration.test.ts`
Expected: 2 个新测试 FAIL（现有占位首页无 `tool-cta`），其余 PASS。

---

### Task 2: 首页内容片段（TDD 绿灯）

**Files:**
- Rewrite: `src/content/home.zh.html`
- Rewrite: `src/content/home.en.html`

- [ ] **Step 1: 重写 `src/content/home.zh.html`**

全文替换为（片段只含正文，无 html/head/body）：

```html
<div class="home-hero">
  <h1>玄命阁</h1>
  <p class="lead">以传统命理与占卜的智慧，观照当下，启迪未来。</p>
</div>

<div class="home-tools">
  <div class="tool-card">
    <h2>八字排盘</h2>
    <p>输入出生时间，排出四柱八字，解读命局走势。</p>
    <p class="tool-features">四柱排盘 · 十神纳音 · 大运流年解读</p>
    <a class="tool-cta" href="/zh/bazi/">开始排盘</a>
  </div>
  <div class="tool-card">
    <h2>六爻起卦</h2>
    <p>铜钱摇卦成卦，以周易卦辞爻辞解读吉凶趋势。</p>
    <p class="tool-features">铜钱起卦 · 六十四卦 · 卦象解读</p>
    <a class="tool-cta" href="/zh/liuyao/">立即起卦</a>
  </div>
</div>

<section>
  <h2>天干地支</h2>
  <p>天干地支是中国传统历法与术数的基本符号：十天干（甲乙丙丁戊己庚辛壬癸）与十二地支（子丑寅卯辰巳午未申酉戌亥）依次相配，组成六十甲子，周而复始。八字排盘以出生时刻的四组干支刻画命局，六爻起卦同样以干支纪时成卦——天干地支是理解这两门学问的共同基础。</p>
</section>

<section>
  <h2>五行</h2>
  <p>金、木、水、火、土五种基本要素，构成了古人认识世界的基本框架。五行之间相生相克：木生火、火生土、土生金、金生水、水生木；木克土、土克水、水克火、火克金、金克木。在八字命理中，每个天干地支都有其五行属性，五行的旺衰平衡是解读命局的重要线索。</p>
</section>
```

- [ ] **Step 2: 重写 `src/content/home.en.html`**

全文替换为（与中文四板块一一对应，措辞按英文习惯撰写）：

```html
<div class="home-hero">
  <h1>Xuanming Pavilion</h1>
  <p class="lead">Time-honoured Chinese fortune-telling and divination — insight into the present, guidance for what lies ahead.</p>
</div>

<div class="home-tools">
  <div class="tool-card">
    <h2>BaZi Chart</h2>
    <p>Enter your birth date and time to build your Four-Pillars chart and read your destiny's course.</p>
    <p class="tool-features">Four Pillars · Ten Gods &amp; Nayin · Luck-cycle readings</p>
    <a class="tool-cta" href="/en/bazi/">Build My Chart</a>
  </div>
  <div class="tool-card">
    <h2>I Ching Casting</h2>
    <p>Toss coins to cast a hexagram and read its meaning through the Zhou Yi judgement and line texts.</p>
    <p class="tool-features">Coin casting · 64 hexagrams · Hexagram readings</p>
    <a class="tool-cta" href="/en/liuyao/">Cast a Hexagram</a>
  </div>
</div>

<section>
  <h2>Heavenly Stems &amp; Earthly Branches</h2>
  <p>The Heavenly Stems and Earthly Branches are the basic symbols of traditional Chinese calendrics and divination: ten stems (Jia, Yi, Bing, Ding, Wu, Ji, Geng, Xin, Ren, Gui) pair with twelve branches (Zi, Chou, Yin, Mao, Chen, Si, Wu, Wei, Shen, You, Xu, Hai) to form the sixty-unit Jiazi cycle. A BaZi chart is built from the four stem-branch pairs at your birth moment, and I Ching casting likewise marks time in stems and branches — the shared foundation of both arts.</p>
</section>

<section>
  <h2>The Five Elements</h2>
  <p>Metal, Wood, Water, Fire and Earth — the five basic elements of the classical Chinese worldview. They generate one another (Wood feeds Fire, Fire creates Earth, Earth bears Metal, Metal collects Water, Water nourishes Wood) and overcome one another (Wood parts Earth, Earth dams Water, Water quenches Fire, Fire melts Metal, Metal chops Wood). In BaZi, every stem and branch carries an elemental nature, and the balance of the five elements is a key thread in reading a chart.</p>
</section>
```

- [ ] **Step 3: 运行集成测试，确认通过**

Run: `npx vitest run test/integration.test.ts`
Expected: 全部 PASS（含 Task 1 新增的 2 个测试）。

- [ ] **Step 4: Commit**

```powershell
git add test/integration.test.ts src/content/home.zh.html src/content/home.en.html
git commit -m "feat(home): rewrite homepage with hero, tool cards and intro sections"
```

---

### Task 3: 首页样式

**Files:**
- Modify: `public/assets/style.css`（文件末尾追加）

- [ ] **Step 1: 在 `style.css` 末尾追加分组**

```css
/* ---------- 首页 ---------- */

.home-hero {
  text-align: center;
  padding: 2.5rem 0 1.5rem;
}

.home-hero h1 { margin-bottom: 0.5rem; }

.home-tools {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 1rem;
  margin: 1.5rem 0 2.5rem;
}

.tool-card {
  display: flex;
  flex-direction: column;
  border: 1px solid #e5ddd2;
  border-radius: 8px;
  background: #fff;
  padding: 1.25rem;
}

.tool-card h2 { margin-top: 0; }

.tool-features {
  color: var(--muted);
  font-size: 0.88rem;
  margin: 0.25rem 0 1rem;
}

.tool-cta {
  margin-top: auto;
  align-self: flex-start;
  background: var(--accent);
  color: #fff;
  border-radius: 6px;
  padding: 0.45rem 1.6rem;
  text-decoration: none;
}

.tool-cta:hover { opacity: 0.9; }

@media (max-width: 560px) {
  .home-tools { grid-template-columns: 1fr; }
}
```

要点：`.tool-card` 视觉与现有 `.bazi-card` 一致（白底、`#e5ddd2` 边框、8px 圆角）；`.tool-cta` 视觉与 `.bazi-submit` 一致（`--accent` 底白字）；`margin-top: auto` 使两张卡片的 CTA 底部对齐。

- [ ] **Step 2: 本地视觉验证**

Run: `npm run dev`（后台运行），浏览器打开 `http://localhost:8787/zh/` 与 `http://localhost:8787/en/`。
检查：Hero 居中；双卡片并排且 CTA 底部对齐；窄窗口（≤ 560px）卡片堆叠为单列；CTA 点击跳转正确。

- [ ] **Step 3: Commit**

```powershell
git add public/assets/style.css
git commit -m "style(home): add hero, tool-card and responsive grid styles"
```

---

### Task 4: Registry meta 更新 + 全量验证

**Files:**
- Modify: `src/pages/registry.ts:33-36`（首页条目的 meta）

- [ ] **Step 1: 更新首页 meta.description**

将 `registry.ts` 中 slug `""` 条目的 meta 从：

```ts
    meta: {
      zh: { title: "首页", description: "玄命阁首页：八字命理、塔罗占卜与传统文化专栏。" },
      en: { title: "Home", description: "Xuanming Pavilion: BaZi, tarot and traditional culture." },
    },
```

改为：

```ts
    meta: {
      zh: { title: "首页", description: "玄命阁：在线八字排盘与六爻起卦，天干地支、五行入门。" },
      en: { title: "Home", description: "Xuanming Pavilion: free BaZi chart reading and I Ching casting, with introductions to Heavenly Stems, Earthly Branches and the Five Elements." },
    },
```

`title` 不变（`buildHead` 自动拼接站点后缀）。

- [ ] **Step 2: 全量测试 + 类型检查**

Run: `npm test`
Expected: 全部 PASS（122+ 个测试）。Windows 上结束时可能出现 miniflare EBUSY 警告，属无害噪音。

Run: `npm run typecheck`
Expected: 无错误。

- [ ] **Step 3: Commit**

```powershell
git add src/pages/registry.ts
git commit -m "fix(home): update homepage meta description to match actual content"
```
